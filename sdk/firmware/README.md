# Firmware SDK

Device SDK: lifecycle, networking, security, OTA, logging, telemetry, power management, sensors, and actuators. The application-facing runtime that sits on top of the Hardware Abstraction Layer (branch 3, Phase 1).

Status: planned. See [../../docs/ROADMAP.md](../../docs/ROADMAP.md).
