# Virtual Device Simulator

Digital-twin engine that runs the same SDK build as physical hardware, with fault injection (battery drain, sensor error, packet loss, outages, attacks) and a virtual network fabric (branch 5, Phase 2).

Status: planned. See [../docs/ROADMAP.md](../docs/ROADMAP.md).
